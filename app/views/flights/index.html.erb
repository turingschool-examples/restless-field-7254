<h3>All Flights</h3>

<% @flights.each do |flight| %>
<div id="flight-<%=flight.id%>">
  <p><%= flight.number %></p>
  <p><%= flight.airline.name %></p>
  <% flight.passengers.each do |ilda| %>
    <%= ilda.name %>
    <%= <%= link_to 'Remove Passenger', path %>%>
  <% end %>
</div>
<% end %>