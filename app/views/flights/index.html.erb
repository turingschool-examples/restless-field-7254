<h1>Flight Index</h1>

<% @flights.each do |flight| %>
  <p>
  <%= flight.number %>
  <%= flight.airline.name %>
  <ul>
  <% flight.passengers.each do |passenger| %>
    <li><%= passenger.name %></li>
  <% end %>
  </ul>
  </p>
<% end %>