<h1>Flights Index</h1>

<% @flights.each_with_index do |flight, index| %>
  <div id="flight-<%= index %>">
    <%= flight.number %>
    <%= flight.airline.name %>
    <% flight.passengers.each do |passenger| %>
      <%= passenger.name %>
    <% end %>
  </div>
<% end %>
